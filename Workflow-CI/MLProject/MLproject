name:  mlflow-ci-cd-project
python_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "diabetes_preprocessed_full.csv"}
      test_size: {type: float, default: 0.2}
      model_type: {type: str, default: "random_forest"}
    command: "python modelling.py --data_file={data_file} --test_size={test_size} --model_type={model_type}"